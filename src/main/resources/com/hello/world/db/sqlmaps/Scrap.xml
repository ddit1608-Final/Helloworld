<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"  "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Scrap">
	
<!-- 	<select id="listAllScrap" parameterClass="testVO" resultClass="work" >
		select *
		from wanted 
		where $type$ like '%'||#key#||'%'  order by TO_NUMBER(wantedAuthNo) desc
	</select>
	
	<select id="totalScrap" parameterClass="testVO" resultClass="int">
		select count(*) from wanted where $type$ like '%'||#key#||'%'
	</select> -->
	
	
	
</sqlMap>